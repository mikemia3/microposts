<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
     </aside>
    
    <div class="col-md-8">
      <h3>フォローしているユーザ一覧</h3>
      <% @user.following_users.each do |following_user|%>
        
        <div class="col-md-4">
          <div class="thumbnail" style="text-align:center;padding-top:10px;">
          
            <div class="caption">
              <p><b><%= link_to "#{following_user[:name]}",user_path(following_user)%></b></p>
              <p><%= following_user[:profile]%></p>
            
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  
<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the Microposts</h1>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>

